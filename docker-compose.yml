version: '3.8'

services:
  seq:
    image: datalust/seq:2025.1
    container_name: stayfit-seq
    environment:
      ACCEPT_EULA: "Y"
    ports:
      - "5341:80"
    volumes:
      - seq_data:/data
    restart: unless-stopped

volumes:
  seq_data:
